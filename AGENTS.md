## Project overview

This project is a pi extension that automatically names tmux windows and pi session titles from coding-session prompts.

## Changelog

- Any change that modifies behavior must include a `CHANGELOG.md` entry under **[Unreleased]** in the same change.

## Release process

For commit/push/tag/GitHub release/npm publish workflow, follow `docs/release-playbook.md`.

## Fast feedback loop (required)

Run tests before handoff.

```bash
npm test
```
